# Example environment variables for the frontend (Vite)
# Image upload is handled by the backend API
# Development Environment Variables
# Copy this to .env for local development

# Backend API URL (development)
VITE_API_URL=http://localhost:6124/api

# Development server settings
VITE_PORT=5244
VITE_HOST=0.0.0.0

# Base path (if app is not at domain root)
# VITE_BASE=/my-app/

# === PRODUCTION / DOCKER ===
# In production/Docker, use these (without VITE_ prefix):
# API_URL=https://api.production.com/api
# PORT=5244
#
# These are injected at container startup via docker-compose or runtime flags.
# See RUNTIME_CONFIG.md for details.

